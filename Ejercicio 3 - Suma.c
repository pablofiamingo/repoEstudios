// Ejercicio 3: Suma

#include <stdio.h>

main ()
{
	int x, y, suma;
	
	x = 3;
	y = 5;
	suma = x + y;
	
	printf (" El resultado de la suma es: %d\n", suma);
	printf ("%d + %d = %d", x, y, suma);	
	
	return 0;
}
